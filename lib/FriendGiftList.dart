import 'package:flutter/material.dart';

class FriendGiftListPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text( 'Gift List')),
        body: Center(child: Text('ay haga'))
    );
  }
}
